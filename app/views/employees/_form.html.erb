<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :employee_id %>
    <%= f.number_field :employee_id %>
  </div>
  </div>
    <div class="field">
    <%= f.label :vacation_days_used %>
    <%= f.number_field :vacation_days_used %>
  </div>
    <div class="field">
    <%= f.label :sick_days_used %>
    <%= f.number_field :sick_days_used %>
  </div>
  <div class="field">
    <%= f.label :hire_date %>
    <%= f.date_select :hire_date, :start_year => 1993, :end_year => Time.now.year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
